<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.web.uts_tekber">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Broadcastreceivers_b"></activity>
        <activity android:name=".Broadcastreceivers_a" />
        <activity android:name=".Contentproviders_b" />
        <activity android:name=".Contentproviders_a" />
        <activity android:name=".Service_b" />
        <activity android:name=".Service_a" />
        <activity android:name=".Intent_b" />
        <activity android:name=".Intent_a" />
        <activity android:name=".Activityb" />
        <activity android:name=".Activitya" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>